<h3>Lesson 17 Exercise - Grow and shrink flex items</h3>
<p><strong>Tasks</strong></p>
<ul>
<li>Find the&nbsp;<code>style.css</code>&nbsp;file, which is inside the&nbsp;<code>css</code>&nbsp;folder.</li>
<li>In&nbsp;<code>style.css</code>, find the style rules for each&nbsp;<strong>child</strong>&nbsp;of the&nbsp;<code>div</code>&nbsp;with the class of&nbsp;<code>container</code>.</li>
<li>Set each child to have a width of 200px.</li>
<li>Save the your work and open in a browser to see your changes.</li>
<li>Save the your work and open in a browser to see your changes.</li>
<li>On any of the child elements, use the&nbsp;<code>flex</code>&nbsp;shorthand property to set a&nbsp;<code>flex-grow</code>&nbsp;of 2 (so the element grows twice as fast as the others), a&nbsp;<code>flex-shrink</code>&nbsp;of 1 (so it shrinks at the same rate), and a&nbsp;<code>flex-basis</code>&nbsp;of 100px.&nbsp;<strong>Hint: the values should appear in that same order in the flex shortcut.</strong></li>
<li>Save the your work and open in a browser to see your changes. Resize the window to see how&nbsp;<code>flex-grow</code>&nbsp;and&nbsp;<code>flex-shrink</code>&nbsp;values affect the result!</li>
</ul>